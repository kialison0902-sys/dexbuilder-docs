---
title: "环境"
description: "配置 API Host、凭证、合约地址与签名 Domain。"
---

## 生产 REST API

`text
https://api.dexbuilder.com/api/v1/
`

交易 API Reference 请求使用该 Base URL。

## Builder 配置

将以下值按环境独立保存：

- REST 与 WebSocket Base URL；
- 支持的网络与抵押资产；
- Vault 与 Verifier 合约地址；
- Broker ID 与管理员账户凭证；
- EIP-712 Domain 与签名规则；
- 限流与维护状态接口。

<Warning>使用当前环境配置的准确 Host、路径、合约地址与签名 Domain。</Warning>

## 环境隔离

Sandbox 与生产环境的 Broker ID、账户、API 凭证、合约地址及签名 Domain 必须相互隔离。
