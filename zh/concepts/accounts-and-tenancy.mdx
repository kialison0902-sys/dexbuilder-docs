---
title: "账户与 Builder 租户"
description: "理解钱包、账户、Builder 与凭证之间的关系。"
---

## 推荐账户模型

一个钱包可能与多个 Builder 交互。每次注册都必须保存用于手续费、收入、分析与访问控制的 Builder 归属。

| 对象 | 所有者 | 包含内容 |
|---|---|---|
| 钱包 | 用户 | 注册与资产移动的签名权限 |
| 账户 | Builder 上下文中的用户 | 余额、订单、仓位、成交和 Trading Key |
| 项目 | Builder | 环境、产品、品牌、域名与部署配置 |
| Admin 凭证 | Builder | 对项目级数据与配置的有限权限 |

## 隔离要求

- Builder 只能访问自己的项目及 Builder 范围内的用户数据。
- 用户资产仍由对应钱包和结算合约控制。
- Builder Admin 不能获得伪造用户钱包签名的能力。
- Sandbox 与生产账户和 Key 必须隔离。
- 每条收入与分析记录必须包含稳定的 Builder 归属。

<Warning>
  不要只根据前端域名或可修改请求头推断 Builder。必须通过已注册账户或已签名操作验证归属。
</Warning>
